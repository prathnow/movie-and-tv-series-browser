class MovieView {
  renderMovieDetails(movieDetails) {
    // Rendering full information about the movie
  }

  renderLatestMovies(movies) {
    // Popular movies component rendering
    console.log(movies);
  }

  renderRecentTrailers(trailers) {
    // Rendering of Recent Trailers component
  }

  renderCarousel(movies) {
    // Rendering a carousel with recent videos
  }
}

export default new MovieView();
